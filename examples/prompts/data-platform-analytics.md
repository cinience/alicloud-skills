# 数据平台与分析

## 目标

完成数据湖治理、分析与安全配置的典型流程。

## 提示词样例

用 `alicloud-data-lake-dlf` 列出当前 Catalog、Database 和 Table 的清单。

用 `alicloud-data-lake-dlf-next` 查询指定数据库的权限配置与授权关系。

用 `alicloud-data-analytics-dataanalysisgbi` 列出可用的数据集与仪表盘。

用 `alicloud-security-kms` 创建一个对称密钥并返回关键参数模板。
